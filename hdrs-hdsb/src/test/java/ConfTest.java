
import com.dslab.hdsb.core.BenchmarkConfig;
import com.dslab.hdsb.distlib.DistProperty;
import java.io.IOException;
import java.io.InputStream;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.Properties;

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/**
 *
 * @author Weli
 */
public class ConfTest {

  public static void main(String[] args) throws IOException {
//    BenchmarkConfig conf = new BenchmarkConfig("F:\\123.property");
//    DistProperty dist = new DistProperty(10);
//    dist.createFileSize(Integer.valueOf(conf.getFileSizeLower()), Integer.valueOf(conf.getFileSizeUpper()),
//            conf.getFileSizeDist(), Double.valueOf(conf.getFileSizeZipfExponent()),
//            Integer.valueOf(conf.getFileSizeNormalSD()));
//
//    dist.createSource(conf.getSourceDist(), conf.getSource(),
//            Double.valueOf(conf.getFileSizeZipfExponent()),
//            Integer.valueOf(conf.getFileSizeNormalSD()));
//    dist.createDestination(conf.getDestinationDist(), conf.getDestination(),
//            Double.valueOf(conf.getFileSizeZipfExponent()),
//            Integer.valueOf(conf.getFileSizeNormalSD()));
//
//    dist.createReqFrequency(conf.getReqFreqDist(), Integer.valueOf(conf.getReqFreqLower()),
//            Integer.valueOf(conf.getReqFreqUpper()), Double.valueOf(conf.getReqFreqZipfExpoent()),
//            Integer.valueOf(conf.getReqFreqNomalSD()));
//    while (!dist.getFrequencyQueue().isEmpty()) {
//      System.out.println(dist.getFrequencyQueue().poll());
//      System.out.println(dist.getFileSizeQueue().poll());
//      System.out.println(dist.getUploadTargetQueue().poll());
//      System.out.println(dist.getDestinationQueue().poll());
//    }

  }
}
